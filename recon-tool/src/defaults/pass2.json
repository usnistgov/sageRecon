{
  "_recon_note": "BUNDLED DEFAULT, compiled into the recon binary. This is the pass-2 semi-enzymatic search on the identified subset. It carries NO database.fasta and NO mzml_paths on purpose: pass 2 always writes the generated subset FASTA and the run's own mzML. precursor_tol, fragment_tol, isotope_errors and the mods are ALSO overridden at runtime from what pass 1 measured \u2014 the values below are the fallback shape, not a record of what ran. That record is the pass2-effective-params.json written beside the search, and Sage's own results.json beside that. Every other field must match testing/configs/digestion-efficiency-pass2.json \u2014 bundled_default_matches_committed_template asserts it.",
  "database": {
    "bucket_size": 8192,
    "enzyme": {
      "missed_cleavages": 1,
      "min_len": 8,
      "max_len": 50,
      "cleave_at": "KR",
      "restrict": "P",
      "semi_enzymatic": true
    },
    "peptide_min_mass": 500.0,
    "peptide_max_mass": 5000.0,
    "ion_kinds": [
      "b",
      "y"
    ],
    "min_ion_index": 2,
    "static_mods": {},
    "variable_mods": {},
    "max_variable_mods": 0,
    "decoy_tag": "rev_",
    "generate_decoys": true
  },
  "precursor_tol": {
    "ppm": [
      -10.0,
      10.0
    ]
  },
  "fragment_tol": {
    "ppm": [
      -20.0,
      20.0
    ]
  },
  "precursor_charge": [
    2,
    4
  ],
  "isotope_errors": [
    0,
    3
  ],
  "deisotope": true,
  "chimera": false,
  "wide_window": false,
  "min_peaks": 15,
  "max_peaks": 150,
  "max_fragment_charge": 1,
  "min_matched_peaks": 4,
  "report_psms": 1,
  "predict_rt": false,
  "protein_grouping": false
}
